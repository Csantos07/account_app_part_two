<%# TODO %>
<h1>Forecast Estimate</h1>

<div class="flex-box">
  <div class="card">
      <div class="card-body">
        <h5 class="card-title"><%= @transaction.name %> (<%= @transaction.balance %>)</h5>

        <p class="card-text"> Account Current Balance: <%= @account_to_forecast.balance %></p>
        <p class="card-text">Account Balance on <%= @date_to_forecast %> : <%= @balance_at_forecast %></p>
      </div>
  </div>
</div>

<%= button_to "Create New transaction", new_transaction_path, method: :get %>
